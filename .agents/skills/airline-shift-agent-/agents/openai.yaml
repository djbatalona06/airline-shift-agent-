interface:
  display_name: "Airline Shift Agent"
  short_description: "Repo-specific patterns and workflows for airline-shift-agent-"
  default_prompt: "Use the airline-shift-agent- repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true